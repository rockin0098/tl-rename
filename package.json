{
	"name": "tl-rename",
	"displayName": "tl-rename",
	"description": "rename tl memeber names",
	"publisher": "rockin0098",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.30.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.tl-rename"
	],
	"main": "./extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.tl-rename",
				"title": "tl-rename"
			}
		],
		"keybindings": [
			{
				"command": "extension.tl-rename",
				"key": "ctrl+shift+a",
				"mac": "ctrl+shift+a"
			}
		]
	},
	"scripts": {
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"typescript": "^3.1.4",
		"vscode": "^1.1.25",
		"eslint": "^4.11.0",
		"@types/node": "^8.10.25",
		"@types/mocha": "^2.2.42"
	}
}